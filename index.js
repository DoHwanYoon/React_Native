/**
 * @format
 */

import {AppRegistry} from 'react-native';
import main from './Study/TodoList/CustomTodoList';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => main);
